import SlackOAuthClient from 'messaging-api-slack';
import React from 'react';
import drive from '../../../apis/drive';


// get access token by setup OAuth & Permissions function to your app.
// https://api.slack.com/docs/oauth

/*
class Drive2 extends React.Component {

    componentDidMount() {
      //   this.onSearchSubmit()
    }


    onSearchSubmit = async () => {
       // const response = await drive.delete('/files/1Zc_LDyXSt9NYtF9dq11PrDwt6vdi1XdX')
    };
    //  this.setState({ images: response.data.results })


    render() {
        return (
            <div>Class</div>
        );
    }
}
export default Drive2;*/
/*
var fileMetadata = {
    'name': 'photo.jpg'
  };
  var media = {
    mimeType: 'image/jpeg',
    body: fs.createReadStream('files/photo.jpg')
  };
  drive.files.create({
    resource: fileMetadata,
    media: media,
    fields: 'id'
  }, function (err, file) {
    if (err) {
      // Handle error
      console.error(err);
    } else {
      console.log('File Id: ', file.id);
    }
  });
  */