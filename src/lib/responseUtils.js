export const responseEmHeight = (win) => {
    let pem = -38.15385 + 0.07051 * win;
    return pem + 'em';
};
export const responseEmWidth = (win) => {
    let pem = -10.764 + 0.046 * win;
    return pem + 'em';
};
export const responsefontHeaderObj = (win) => {
    let pem = -14.425 + 0.371 * win;
    return pem + 'em';
};
